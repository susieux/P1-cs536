
@SuppressWarnings("serial")
public class DuplicateSymException extends Exception {

//	These two classes (which must be in files named DuplicateSymException.java and EmptySymTableException.java) will simply define 
//	the two checked exceptions that can be thrown by the SymTable class. Each exception must be able to be created using a constructor that takes no arguments.
//	To define a checked exception named XXX, you can use code like this:
//	    public class XXX extends Exception {
//	    }
//	Note that the class has an empty body (it will have a no-argument constructor by default).

}
